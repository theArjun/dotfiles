name: easysheet
root: ~/workspace/wesionary/

startup_window: editor

windows:
  - editor:
      root: ~/workspace/wesionary/easysheet
      panes:
        - nvim .
  - backend:
      root: ~/workspace/wesionary/easysheet
      panes:
        - uvicorn app.main:easy_sheet --reload
  - frontend:
      root: ~/workspace/wesionary/easysheet-lp
      panes:
        - npm run dev
  - admin:
      root: ~/workspace/wesionary/easysheet-admin
      panes:
        - npm run dev
  - github-dashboard:
      panes:
        - gh dash
